# 캠페인 스케줄 관리 대시보드 — 작업 이력

KAIST · SK브로드밴드 12팀 (써니C)
2026년 7월 28일 ~ 8월 2일 · 커밋 120개

---

## 1. 무엇을 만들고 있나

SK브로드밴드는 고객에게 마케팅 메시지(쿠폰, 프로모션 안내 등)를 보냅니다.
지금은 **구글 스프레드시트**로 그 일정을 관리합니다.

그래서 이런 일이 생깁니다.

- 마케터는 원하는 날에 **보낼 여유가 있는지** 알 수 없습니다
- 담당자는 **어느 날에 얼마나 몰렸는지** 한눈에 못 봅니다
- 일정이 겹치면 **누가 언제 조정했는지** 기록이 남지 않습니다

이 대시보드는 그 자리를 대신합니다. 마케터가 **가능한 날을 찾아 신청**하고,
담당자가 **발송 시각을 배정해 확정**하는 흐름입니다.

### 등장인물

| | 하는 일 |
|---|---|
| **마케터** | 캠페인을 기획하고 발송을 신청합니다. 여러 팀(A·B·C)에 흩어져 있습니다 |
| **담당자** | 모든 신청을 보고 발송 시각을 배정해 확정합니다. 캐파를 관리합니다 |

---

## 2. 반드시 알아야 할 규칙

이 프로젝트에서 가장 오래 걸린 것은 코드가 아니라 **규칙을 정확히 알아내는 일**이었습니다.
미팅 녹음과 담당자 확인을 거치며 여러 번 뒤집혔습니다.

### 캐파 — 시스템이 막는 유일한 제약

하루에 내보낼 수 있는 양에 한도가 있습니다.

| 채널 | 하루 한도 |
|---|---|
| 배너 | **400만 건** |
| 쿠폰 (오퍼) | **440만 건** |
| TV팝업 | 한도 없음 |
| 토스트팝업 | 한도 없음 |

> **시간대는 제약이 아닙니다.**
> 같은 시각에 같은 채널이 여럿 나가도 캐파만 안 넘으면 됩니다 (7/30 담당자 확인).
> 시간대는 "막느냐"가 아니라 "어떻게 배치하느냐"의 문제입니다.
> 이걸 잘못 알아 한동안 시간대를 잠그는 코드를 만들었다가 걷어냈습니다.

### 발송 주기

| | 뜻 | 캐파를 쓰는 날 |
|---|---|---|
| **일회성** | 시작일에 **한 번** 보냅니다. 노출은 그 뒤로도 이어집니다 | 시작일 **하루만** |
| **주기성** | 기간 내내 **매일** 보냅니다 | **기간 전체 매일** |

주기성은 17~19시 중에서 배정합니다.

### 상태 — 다섯 가지뿐이고 늘리지 않습니다

```
        마케터 신청
             │
             ├─ 캐파 여유 있음 ──────→ 신청 (화면에는 '확정 대기')
             │                            │
             └─ 캐파 부족, 그 날을 고수 ─→ 조정 필요
                                          │
                        담당자가 처리 ────┤
                                          ├──→ 확정 ──→ 발송 완료
                                          └──→ 취소
```

| 상태 | 뜻 |
|---|---|
| **신청** | 올라왔고 담당자의 확정을 기다립니다. 화면에는 `확정 대기`로 씁니다 |
| **조정 필요** | 캐파가 모자란 날을 마케터가 사유를 적어 고수한 건입니다 |
| **확정** | 담당자가 발송일과 시각을 정했습니다 |
| **발송 완료** | 실제 발송이 끝난 뒤 발송 시스템이 찍습니다. 이 앱은 이 전이를 만들지 않습니다 |
| **취소** | 마케터가 철회했거나, 담당자가 협의 후 내렸습니다 |

### 마케터는 시간대를 고르지 않습니다

7/31 피드백으로 정해졌습니다. 마케터는 **날짜만** 고르고,
**시각은 담당자가 확정하면서 배정**합니다. 마케터에게 시간대를 보여주면
"내가 뭘 해야 하나" 싶은데 실제로 할 수 있는 게 없기 때문입니다.

### 취소는 함부로 못 합니다

| 누가 | 언제 | 사유 |
|---|---|---|
| 마케터 | 본인 건이 **확정 전**일 때 | 필요 없음 (스스로 철회) |
| 담당자 | 확정 대기·조정 필요는 **바로** | **필수** |
| 담당자 | 확정 건은 **`수정하기`를 거친 뒤** | **필수** |

확정 건에 한 단계를 더 둔 이유는 **이미 마케터에게 확정을 알린 건**이기 때문입니다.

### 헷갈리기 쉬운 두 숫자

같은 날짜에 대해 **서로 다른 숫자**를 씁니다. 쓰임이 다르기 때문입니다.

| | 무엇을 세나 | 어디에 쓰나 |
|---|---|---|
| **점유** | 확정 · 발송 완료 · 신청 | "여기 넣을 자리가 있나" 판정 |
| **요약** | 취소를 뺀 전부 (**조정 필요 포함**) | 캘린더 칸에 적는 숫자 |

조정 필요를 점유에서 빼는 이유 — 그걸 세면 같은 날 **확정 건을 열었을 때
멀쩡한 그 건이 캐파를 넘긴 것처럼** 보입니다. 문제는 아직 자리를 못 잡은
조정 필요 건이지 확정 건이 아닙니다.

요약에는 넣는 이유 — 그 칸에 **나열된 건들의 합**과 숫자가 맞아야 하기 때문입니다.

---

## 3. 어떻게 여기까지 왔나

날짜별로 무엇이 바뀌었는지입니다. **뒤집힌 결정**을 특히 적어 둡니다.

### 7/28 — 시작

첫 프로토타입. 3탭 구조(캠페인 신청 · 캠페인 관리 · 캘린더).

### 7/29 — 마케터 화면 골격

신청 화면을 좌우 2단으로. 조정 요청 경로를 처음 넣었습니다.

### 7/30 — 규칙을 파고든 날 (커밋 40여 개)

미팅 녹음을 다시 듣고 **일회성/주기성**을 제대로 구현했습니다.
주기성이 기간 내내 캐파를 쓴다는 것, 캘린더에 연속 막대로 그려야 한다는 것.

이 날 **선점(先占)** 개념을 만들었습니다 — "그 시간대를 남이 잡고 있다".
담당자가 선점 건을 옮겨 자리를 내주는 화면까지 만들었습니다.

> **나중에 뒤집힘** — 시간대는 제약이 아니었습니다. 선점 개념 자체를 8/1에 걷어냈습니다.
> 다만 **"자리를 만들려고 다른 건을 옮긴다"는 흐름은 좋은 것**이어서,
> 8/2에 **캐파 기준**으로 되살렸습니다.

담당자 화면(캠페인 관리)을 처음 만들고, `반려`를 없애고 `신청 취소`로 통일했습니다.

### 7/31 — 데모 피드백 반영

팀에서 받은 피드백 7가지를 반영했습니다.

- 신청 화면 위쪽 **요약 3칸 제거** — 어차피 아래 캘린더를 보고 고르니까
- **마케터가 시간대를 고르지 않게** → 담당자가 확정하며 배정
- 담당자 화면을 **캘린더 한 장**으로 (리스트를 따로 보지 않게)
- 담당자 탭을 누르면 **캠페인 관리가 기본**
- **담당자 → 마케터 조정 요청 경로 삭제** — 담당자가 그냥 조정해서 확정으로

가장 큰 구조 변경은 **기간의 의미**였습니다.
원래 `시작일·종료일`이 조회 범위이자 캠페인 기간이었는데, 그러면
중간 날짜를 고를 때 마케터가 정하지 않은 종료일이 생겼습니다.

```
조회 기간 (언제부터 언제 사이에 시작하고 싶은가)
      +
캠페인 일수 (며칠짜리인가)
      ↓
달력에서 시작일을 고르면 → 종료일이 계산됨
```

### 8/1 — 마케터 UT · 세부 다듬기

`prototype0801_code.html`로 **마케터 뷰 UT**를 진행했습니다.
UT 시나리오 문서(`UT 수정 후.docx`)도 현재 흐름에 맞게 다시 썼습니다.

UT에서 나온 피드백:
- **잔여량이 다른 정보에 묻혀 안 보임** → 날짜 옆으로 빼서 크게
- `이 날 시작` → **`이 날짜 선택하기`** (실제로는 날짜만 고르는 버튼이라)
- `이 신청 내용이 맞으십니까?` → **`신청 내용을 확인해 주세요`**
- `다시 확인` → **`수정하기`**

여백을 12·8 리듬으로 정리했습니다 (8은 묶이는 것끼리, 12는 성격이 바뀌는 자리).

### 8/2 — 담당자 화면 완성 · 데이터 · 웹앱 전환

담당자 화면에 **선점 건 이동을 캐파 기준으로 되살렸습니다.**

```
조정 필요 클릭
  └ 배너 캐파 80만 초과
  └ 확정 건 일정 변경
       8월 신규 요금제 안내  150만  10:00  [일정 조정]
       여름 결산 특별전       90만  14:00  [일정 조정]
         └ [일정 조정] 누르면 그 건의 날짜·시간이 아래로 펼쳐짐
            → [두 건 함께 확정]
```

옮길 날짜를 고르는 순간 **위 캐파 게이지가 즉시 내려갑니다.**
옮기는 중에는 단독 확정을 막아서, 옮기다 만 채로 확정되지 않습니다.

**더미 데이터를 204건으로** 늘렸습니다. 그전에는 8월 배너 사용률이 평균 7%라
캘린더가 비어 보이고 추천이 갈리지 않았습니다.

그리고 **데이터를 실제 DB(Supabase)로** 옮기고, **웹앱(Next.js)으로 전환**을 시작했습니다.

---

## 4. 왜 웹앱으로 옮겼나

HTML 한 파일로도 여기까지 왔는데 왜 바꾸는지, 근거가 있습니다.

앞으로 붙일 것이 **로그인 · 실시간 연동 · 메일 자동 발송** 셋인데,
정확히 프레임워크와 서버가 있어야 편한 것들입니다.
바닐라로 만들고 나중에 옮기면 같은 일을 두 번 합니다.

**결정적인 이유는 따로 있었습니다.** 실시간이 붙으면 남의 변경이 올 때마다
화면을 통째로 다시 그리는데, 그때 입력 중이면 이렇게 됩니다.

| | |
|---|---|
| 입력한 값 | 살아남음 |
| **포커스** | **날아감** |
| **커서 위치** | **날아감** (4 → 0) |

마케터가 캠페인명을 치는 중에 담당자가 다른 건을 확정하면 커서가 튕겨 나갑니다.
두 사람이 나란히 앉아 찍는 라이브 데모에서 바로 보일 결함입니다.

이걸 지금 구조에서 고치려면 부분 갱신을 손으로 짜야 하는데,
그건 프레임워크를 어설프게 다시 만드는 일입니다.

### 옮기면서 지킨 것

**HTML 프로토타입을 지우지 않았습니다.** 사양서 겸 UT 도구로 남깁니다.
날짜별 스냅샷을 파일로 열어 나란히 비교하는 방식이 이 프로젝트의 리뷰 방법이었고,
UT는 네트워크 없이 열려야 하기 때문입니다.

---

## 5. 지금 무엇이 있나

```
prototype/
├── prototype0728~0802_code.html   날짜별 스냅샷. 열면 바로 돕니다
│                                   0802 가 최신 · 앞의 것은 비교용으로 얼려 둠
├── config.js                       HTML 용 Supabase 접속 정보 (비어 있음)
├── index.html                      초기 버전 (별도 계보)
│
├── supabase/
│   ├── 01_schema.sql              표·인덱스·제약·실시간·권한
│   ├── 02_seed.sql                캠페인 204건. 다시 실행해도 안전
│   └── README.md                  적용 절차
│
└── webapp/                         새 웹앱 (Next.js 16 · React 19 · TypeScript)
    ├── lib/domain.ts              ★ 도메인 규칙. 프레임워크 무관
    ├── lib/domain.parity.ts       프로토타입과 값이 같은지 대조
    ├── lib/db.ts                  Supabase 연결
    ├── lib/db.check.ts            연결 점검
    ├── lib/useCampaigns.ts        읽기 · 쓰기 · 실시간
    ├── components/ManageCalendar.tsx
    ├── app/                       담당자 화면
    └── .env.local                 접속 정보 (git 에 안 올라감)
```

### `lib/domain.ts` 가 가장 중요합니다

React 도 Supabase 도 DOM 도 모르는 **순수 함수만** 있습니다.
현업 개발팀이 다른 언어·다른 틀로 다시 만들더라도 **이 파일의 규칙만 옮기면
동작이 같아집니다.** 코드보다 "왜 그런지"를 주석으로 적어 두었습니다.

### 검증 두 가지

| 명령 | 확인하는 것 |
|---|---|
| `npm run parity` | 도메인 모듈이 HTML 프로토타입과 **같은 값**을 내는가 — **1,312항목 일치** |
| `npm run db:check` | DB 연결 · 204건 적재 · 값이 프로토타입과 같은가 |

포팅에서 제일 위험한 것이 "비슷해 보이지만 미묘하게 다른 계산"인데,
그걸 기계로 막아 두었습니다.

---

## 6. 돌려보는 법

### HTML 프로토타입 (설치 없음)

```
prototype0802_code.html 을 브라우저로 엽니다.
```

네트워크 없이 돕니다. 데이터는 파일 안에 들어 있어 새로고침하면 초기화됩니다.

### 웹앱

```bash
cd prototype/webapp
npm install
npm run dev          # http://localhost:3000
```

`.env.local` 에 Supabase 주소와 키가 있어야 데이터가 올라옵니다.
없으면 빈 화면이 뜹니다.

### Supabase 처음 설정할 때

1. supabase.com 에서 프로젝트 생성 (무료 · 지역은 Seoul 권장)
2. SQL Editor 에서 `supabase/01_schema.sql` → `02_seed.sql` 순서로 실행
3. Project Settings → API 에서 값을 복사해 `webapp/.env.local` 에 넣기

자세한 절차는 `supabase/README.md` 에 있습니다.

---

## 7. 남은 일

| | 내용 |
|---|---|
| **담당자 처리 팝업** | 확정 · 취소 · 일정 조정 · 두 건 함께 확정. 웹앱으로 이식 중 |
| **마케터 화면** | 조회 → 달력 → 추천 → 신청. 웹앱으로 이식 |
| **로그인** | ID 로 역할(마케터/담당자)과 팀이 정해짐 |
| **추천 알고리즘** | 지금은 임시 가중치. 아래 참고 |
| **메일 자동 발송** | 시나리오 6가지 정의 → 화면 미리보기 → 실제 발송 |
| **현업 이관 문서** | 스키마 · 상태 전이 · 캐파 근거 · 배포 절차 |

### 추천 알고리즘 — 알려진 한계

지금 점수는 `캐파 여유 0.5 + 준비 기간 0.3 + 평일 여부 0.2` 입니다.
**임시 가중치이며 아직 설계 중입니다.**

- `준비 기간`은 시작일이 **3일 이상 뒤면 전부 1로 포화**됩니다
- `캐파 여유`는 그 날에 **캠페인이 없으면 값이 같아집니다**

데이터가 한산하면 세 후보의 점수가 같아져 날짜순으로만 갈렸습니다.
더미를 204건으로 늘린 뒤로는 갈리기 시작했습니다 (`0.802 / 0.765 / 0.719`).

---

## 8. 지금 보안 상태 — 읽어 주세요

저장소가 **공개(public)** 이고 로그인이 아직 없습니다.
그래서 Supabase 접근 키를 본 사람은 **데이터를 넣고 고칠 수 있습니다.**

최악의 경우를 줄이려고 **지우기(delete)만은 막아 두었습니다.**

| 읽기 | 넣기 | 고치기 | 지우기 |
|---|---|---|---|
| 허용 | 허용 | 허용 | **없음** |

통째로 사라지지는 않고, `02_seed.sql` 을 다시 돌리면 204건으로 복구됩니다.
데모·내부 검토용으로는 이 정도면 되지만, **실제 운영 전에는 반드시**
로그인을 붙이고 권한을 역할별로 좁혀야 합니다.

---

## 참고 문서

| 파일 | 내용 |
|---|---|
| `../12팀 데모 피드백_0731.pdf` | 7/31 데모에서 받은 피드백 원문 |
| `../데모 피드백_260728 회의.md` | 7/28 미팅 정리 |
| `../캠페인_상태_정의_260731.md` | 상태 정의 |
| `../캠페인_프로토타입_데이터_260731.xlsx` | 데이터 스키마 (백엔드 요청용) |
| `../UT 수정 후.docx` | UT 시나리오 (현재 흐름 반영) |
