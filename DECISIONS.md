# 결정 기록

바뀐 규칙과 **왜 그렇게 정했는지**를 남깁니다.
코드는 다시 짜도 되지만 이 판단은 다시 못 만듭니다. 미팅과 담당자 확인을 거쳐 나온 것들입니다.

**뒤집힌 결정을 특히 적습니다.** 이유를 모르면 다음 사람이 같은 길을 또 갑니다.

읽는 법 — 각 항목은 `무엇을 정했나 / 왜 / 뒤집힌 경우 그 경위` 순입니다.

---

## D-01 · 시간대는 제약이 아니다

**정한 것** — 같은 시각에 같은 채널이 여럿 나가도 된다. 시스템이 막는 것은 **하루 캐파 하나뿐**이다.

**왜** — 7/30 담당자 확인. 노출 위치가 달라 동시 발송이 가능하다.

**뒤집힌 경위** — 7/30에 「선점(先占)」 개념을 만들고, 같은 시간대를 잠그는 코드와
담당자가 선점 건을 옮겨 자리를 내주는 화면까지 만들었다. 8/1에 사용자가
*"같은 채널 내에서 캐파만 맞으면 무조건 송출 가능이잖아"* 라고 확인해 주면서 전부 걷어냈다.

**남긴 것** — 「자리를 만들려고 다른 건을 옮긴다」는 **흐름 자체는 좋았다.**
8/2에 시간대가 아니라 **캐파 기준**으로 되살렸다. 개념이 틀렸다고 UX까지 버리지 않았다.

---

## D-02 · 마케터는 시간대를 고르지 않는다

**정한 것** — 마케터는 **날짜만** 고른다. 시각은 담당자가 확정하며 배정한다.

**왜** — 7/31 데모 피드백. *"마케터가 시간대 신청은 안 하는 게 좋겠음
(마케터는 잔여 Capa 가 있는 날짜만 알 수 있게)"*

**따라온 것** — 마케터에게 시간대를 보여주면 "내가 뭘 해야 하나" 싶은데 실제로 할 수 있는 게 없다.
그래서 마케터 화면에서 시간 관련 표시를 전부 뺐고, 신청 직후 상태는 `확정 대기` · 시각은 `시간 미정`이다.

---

## D-03 · 조정 필요는 캐파를 점유하지 않는다

**정한 것** — 그 날 "넣을 자리가 있나" 판정에서 `조정 필요` 건은 **뺀다.**

**왜** — 조정 필요는 아직 자리를 못 잡은 건이다. 이걸 세면 같은 날 **확정 건을 열었을 때
멀쩡한 그 건이 캐파를 넘긴 것처럼** 보인다. 문제는 조정 필요 건이지 확정 건이 아니다.

**주의** — **캘린더 요약 숫자는 반대로 조정 필요를 포함한다.** 그 칸에 나열된 건들의
합과 숫자가 맞아야 하기 때문이다. 같은 날짜에 대해 두 숫자가 다른 것은 **쓰임이 달라서**다.

| | 세는 것 | 쓰임 |
|---|---|---|
| 점유 | 확정 · 발송 완료 · 신청 | "넣을 자리가 있나" |
| 요약 | 취소를 뺀 전부 | 캘린더 칸 숫자 |

---

## D-04 · 상태는 다섯 가지뿐이고 늘리지 않는다

**정한 것** — `신청` · `조정 필요` · `확정` · `발송 완료` · `취소`.

**왜** — 8/1 사용자 지시. *"신청한 캠페인 상태에 대해 너무 많은 경우의 수를 두는 것도 좋지 않아."*

**따라온 것** — 담당자가 마케터에게 되돌려 보내는 경로를 없앴다(7/31 피드백).
담당자는 **조정해서 확정하거나 취소하거나** 둘 중 하나다. `반려` 상태도 만들었다가
`신청 취소`로 합쳤다.

**화면 표기** — `신청`은 화면에서 `확정 대기`로 쓴다. 내부 값과 표기가 다른 유일한 경우다.

---

## D-05 · 취소 권한은 단계로 나눈다

| 누가 | 언제 | 사유 |
|---|---|---|
| 마케터 | 본인 건이 확정 전일 때 | 없음 (스스로 철회) |
| 담당자 | 확정 대기 · 조정 필요 → 바로 | **필수** |
| 담당자 | 확정 건 → `수정하기`를 거친 뒤 | **필수** |

**왜** — 확정 건에 한 단계를 더 둔 이유는 **이미 마케터에게 확정을 알린 건**이기 때문이다.
0728 미팅에서 *"취소는 보통 안 하긴 해요"* 라는 언급이 있었다.

---

## D-06 · 조회 기간과 캠페인 기간을 분리한다

**정한 것** — 마케터는 `조회 기간`(언제부터 언제 사이에 시작하고 싶은가)과
`캠페인 일수`를 따로 넣는다. 달력에서 시작일을 고르면 종료일이 계산된다.

**왜** — 원래 `시작일·종료일`이 조회 범위이자 캠페인 기간이었다. 그러면 중간 날짜를 고를 때
**마케터가 정하지 않은 종료일**이 생기고, 「일회성 = 시작일 1회 발송」 규칙과도 어긋났다.

**부수 효과** — 조회 기간이 생기면서 **추천할 후보 날짜의 범위**가 정의됐다.
그전에는 ±3일 대안뿐이라 점수를 매길 대상이 없었다.

---

## D-07 · 발송 시점 선호 (8/2 확인)

| | 선호 |
|---|---|
| 요일 | **금 · 토 · 일** |
| 시각 | **퇴근 후 저녁** — 담당자 배정의 기준 |
| 경쟁 밀도 | **낮을수록 좋음** |
| 준비 기간 | **D-2 이상 권고** (금지 아님) |

**주의** — 초기 코드는 `wk = 주말 ? 0 : 1` 로 **평일을 선호**하게 되어 있었다. **반대다.**
추천 점수를 고칠 때 방향을 뒤집어야 한다.

**D-1 신청은 막지 않는다.** 대신 **담당자에게 메일이 발송되어야 한다.**
문제는 "촉박한 건이 들어오는 것"이 아니라 "담당자가 못 보고 지나치는 것"이라,
제약이 아니라 알림으로 푼다.

---

## D-08 · 데이터 계층을 화면에서 분리한다

**정한 것** — 데이터를 바꾸는 일은 전부 한곳(`DB` / `useCampaigns`)을 거친다.

**왜** — 로그인·실시간 연동을 붙이려면 "누가 언제 무엇을 바꿨는지"가 한곳에 모여야 한다.
화면 곳곳에서 배열을 직접 주무르면 서버로 옮길 때 손댈 곳이 흩어진다.

**변경 지점은 다섯 곳뿐이다** — 마케터 신청·수정, 마케터 취소, 담당자 확정, 담당자 취소,
두 건 함께 확정. 이 다섯이 곧 서버에 쓰는 지점이다.

**두 건 함께 확정이 따로 있는 이유** — 조정 요청 수락과 확정 건 이동이 나뉘면
그 사이에 그 날 캐파가 어긋난다. 실시간이 붙으면 상대 화면에 중간 상태가 보인다.

---

## D-09 · 웹앱으로 옮긴다 (8/2)

**정한 것** — Next.js + TypeScript + Supabase 로 옮긴다. HTML 프로토타입은 **사양서 겸 UT 도구로 남긴다.**

**왜** — 앞으로 붙일 것이 로그인·실시간·메일 셋인데 정확히 프레임워크와 서버가 필요한 것들이다.
바닐라로 만들고 나중에 옮기면 같은 일을 두 번 한다.

**결정적 근거** — 실시간이 붙으면 남의 변경이 올 때마다 화면을 통째로 다시 그린다.
그때 입력 중이면 **포커스와 커서가 날아간다** (실측: 커서 4 → 0).
마케터가 캠페인명을 치는 중에 담당자가 다른 건을 확정하면 커서가 튕긴다.
손으로 부분 갱신을 짜는 것은 프레임워크를 어설프게 다시 만드는 일이다.

**기술을 고른 기준** — 현업 개발팀이 받아 다시 만든다는 전제라, **화려한 것보다 표준적인 것.**
진짜 산출물은 코드가 아니라 `lib/domain.ts` 의 규칙과 이 문서다.

---

## D-10 · 스냅샷을 날짜별로 남긴다

**정한 것** — `prototype0728 ~ 0802_code.html`. 각 날짜의 상태를 얼려서 보관한다.

**왜** — 리뷰가 "어제 것과 오늘 것을 나란히 열어 비교"하는 방식으로 이뤄졌다.
빌드가 필요 없는 단일 파일이라 **더블클릭하면 바로 열린다.** UT도 네트워크 없이 돌아야 했다.

**주의** — 얼린 파일은 **고치지 않는다.** 8/1에 0731v2를 실수로 계속 고쳐서 0801과
바이트 단위로 같아진 적이 있다. 비교 대상이 사라지므로 되돌렸다.

**언젠가 정리해야 한다** — 파일이 계속 늘어난다. 최종본과 주요 분기점만 남기고
나머지는 git 태그로 옮기는 것이 낫다.

---

## D-11 · 추천 근거는 화면에 적지 않는다 (8/6)

**정한 것** — 혼잡도(같은 날 같은 채널 건수)는 점수에만 쓰고 추천 카드에는 표시하지 않는다.
카드는 지금대로 **날짜 · 잔여 · 기간**만 보여준다.

**왜** — 세 후보는 **모두 신청 가능한 날**이다. 어느 것을 골라도 막히지 않는다.
순위의 이유를 설명하는 것보다 화면이 조용한 쪽을 택했다.

**「잔여로 유추할 수 있다」는 근거로 쓰지 않는다** — 검토 때 그렇게 적었으나 측정 결과 틀렸다.
1위가 평일인 23건 중 설명이 필요한 20건에서 **잔여로 유추 가능한 것은 6건(30%)** 이다.
13건은 잔여가 아예 안 뜨고, 1건은 잔여가 오히려 반대다.

**한계 — 알고 받아들인다.**

| | |
|---|---|
| 잔여가 안 뜨는 조회 | 273건 중 **96건(35%)** · TV팝업·토스트팝업만 쓰고 쿠폰이 없으면 캐파 한도가 없다 |
| 순위와 잔여 순서가 반대인 조회 | 잔여가 뜨는 177건 중 **71건(40%)** (전체 기준 26%) · 혼잡도 0.40 > 캐파 여유 0.25 이므로 당연하다 |

**마케터는 대부분의 경우 순위의 이유를 읽어낼 수 없다.** 그것을 알고 정한 결정이다.

**같이 정한 것** — `추천_알고리즘_260802.md` §5 의 「남은 과제」를 이 결정으로 교체했다.
열린 할 일로 두면 문서를 읽는 사람에게 미완성으로 읽히고, 인수인계 받은 사람이 착수한다.

**이 결정이 막지 않는 것** — 담당자가 날짜에 `권장`/`지양`을 찍고 **사유 한 줄**을 다는
설계(T-01)는 성격이 다르다. 계산값이 아니라 사람이 쓴 문장이므로 충돌하지 않는다.

**근거** — `회의/AI멘토링_260806.md` Q4 · Q5
