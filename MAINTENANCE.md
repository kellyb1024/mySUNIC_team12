# 유지·보수 안내

이 프로젝트를 **이어받는 사람**을 위한 문서입니다.
"어떻게 만들었나"는 [history.md](history.md), "왜 그렇게 정했나"는 [DECISIONS.md](DECISIONS.md) 를 보세요.

---

## 1. 코드를 고치기 전에

### 규칙은 한곳에만 있어야 합니다

캐파 계산·상태 판정·추천 점수 같은 **규칙**은 `webapp/lib/domain.ts` 에만 둡니다.
화면 코드에 같은 계산을 다시 쓰면 두 곳이 갈라집니다.

`domain.ts` 는 React 도 Supabase 도 DOM 도 모릅니다. 그래서 다른 언어·다른 틀로
다시 만들어도 이 파일의 규칙만 옮기면 동작이 같아집니다.

### 고친 뒤 반드시 돌리는 것

```bash
cd webapp
npm run parity    # 규칙이 HTML 프로토타입과 같은 값을 내는가 (1,087항목)
npm run db:check  # DB 연결 · 데이터 적재 · 값 일치
npx tsc --noEmit  # 타입
npm run build     # 빌드
```

`parity` 가 깨지면 **규칙이 바뀐 것**입니다. 의도한 변경이면 기준값을 다시 뜨고,
아니면 되돌리세요. 포팅에서 가장 위험한 것이 "비슷해 보이지만 미묘하게 다른 계산"입니다.

기준값을 다시 뜨는 법은 아래 **4. 검증 기준값 갱신** 을 보세요.

---

## 2. 자주 바뀌는 값은 어디에 있나

| 무엇 | 어디 | 개발자 없이 바꿀 수 있나 |
|---|---|---|
| 캐파 한도 (배너 400만 등) | Supabase `capa` 표 | **가능** — 표만 고치면 됨 |
| 오늘 날짜 · 데이터 범위 | `webapp/lib/config.ts` | 코드 수정 필요 |
| 발송 가능 시각 (1~22시) | `webapp/lib/domain.ts` `HOURS` | 코드 수정 필요 |
| 주기성 배정 시각 (17~19시) | `webapp/lib/domain.ts` `REC_HOURS` | 코드 수정 필요 |
| 추천 가중치 | `webapp/lib/domain.ts` `REC_W` | 코드 수정 필요 |

> **캐파는 이미 표로 빼 두었습니다.** 현업에서 가장 자주 바뀔 값이라 그렇게 했습니다.
> 나머지도 필요해지면 같은 방식으로 표로 옮기면 됩니다 — `fetchCapa()` 를 흉내 내세요.

---

## 3. 데이터를 손봐야 할 때

### 처음부터 다시 넣기

```sql
truncate campaigns;
-- 그다음 supabase/02_seed.sql 실행
```

`02_seed.sql` 은 `no` 가 같으면 덮어쓰므로 **여러 번 실행해도 안전**합니다.

### 더미를 새로 만들기

생성기가 `webapp` 밖 작업 폴더에 있었고 저장소에는 없습니다.
다시 만들어야 하면 **규칙을 지켜야** 합니다.

- 확정·신청만으로는 **절대 캐파를 넘기지 않습니다.** 넘기는 것은 `조정 필요` 건뿐입니다
- `조정 필요` 건의 `reason` 은 **그 건을 뺀 실제 잔여**와 문구가 일치해야 합니다
  (예: 잔여 20만인데 100만이 필요 → `8/3(월) 배너 잔여 20만`)
- 지난 날짜에 `신청` · `조정 필요` 가 남아 있으면 안 됩니다
- 확정·발송 완료는 `hour` 가 있고, 신청·조정 필요는 `hour` 가 `null` 입니다

이 규칙이 깨지면 화면이 이상해 보이는 게 아니라 **설명이 안 되는 상태**가 됩니다.

---

## 4. 검증 기준값 갱신

`npm run parity` 는 `/tmp/reference.json` 과 `/tmp/campaigns.json` 을 씁니다.
임시 폴더라 재부팅하면 사라집니다. 다시 뜨는 법:

1. `prototype0802_code.html` 을 복사해 `</body>` 앞에 스크립트를 넣고
   `requests` 와 날짜별 계산 결과를 `document.title` 로 내보냅니다
2. 헤드리스 Chrome `--dump-dom` 으로 읽어 JSON 으로 저장합니다

> **더 나은 방법** — 이 두 파일을 `webapp/fixtures/` 에 커밋해 두면
> 임시 폴더에 기대지 않아도 됩니다. 아직 안 했습니다.

---

## 5. 배포

| 무엇 | 어디 | 어떻게 |
|---|---|---|
| HTML 프로토타입 | GitHub Pages | `main` 에 푸시하면 자동 |
| 웹앱 | **아직 없음** | Vercel 권장 (Next.js 라 Pages 로는 불가) |

### HTML 배포 확인

푸시 후 반영까지 시간이 걸립니다. 브라우저 캐시도 있으니 이렇게 확인하세요.

```bash
# 로컬과 서버 파일이 같은지
shasum -a 256 prototype0802_code.html | cut -c1-12
curl -s -H 'Cache-Control: no-cache' \
  "https://kellyb1024.github.io/mySUNIC_team12/prototype0802_code.html?v=$RANDOM" \
  | shasum -a 256 | cut -c1-12
```

두 값이 같아야 배포된 것입니다. **다르면 아직 반영 전이거나 푸시가 안 된 것**입니다.

### 웹앱 배포할 때

`.env.local` 은 커밋되지 않으므로 **Vercel 대시보드에 환경변수를 따로 넣어야** 합니다.

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

Root Directory 는 `prototype/webapp` 입니다.

---

## 6. 보안 — 지금 상태와 해야 할 일

### 지금

저장소가 **공개**이고 로그인이 없습니다. Supabase 접근 키가 공개되므로
**키를 본 사람이 데이터를 넣고 고칠 수 있습니다.**

최악을 줄이려고 **지우기(delete) 정책을 아예 만들지 않았습니다.**

| 읽기 | 넣기 | 고치기 | 지우기 |
|---|---|---|---|
| 허용 | 허용 | 허용 | **없음** |

통째로 사라지지는 않고 `02_seed.sql` 로 복구됩니다.

### 실제 운영 전에 반드시

1. **로그인을 붙입니다** (Supabase Auth)
2. `01_schema.sql` 의 정책을 역할별로 좁힙니다
   - 마케터 — 전체 읽기, **본인 건만** 쓰기
   - 담당자 — 전체 읽기·쓰기
3. 키가 샜다면 대시보드에서 **새로 발급**하고 `.env.local` / Vercel 환경변수만 바꿉니다

> `service_role`(신규 이름 `secret`) 키는 **어떤 경우에도 클라이언트에 넣지 않습니다.**
> 서버에서만 쓸 일이 생기면 `NEXT_PUBLIC_` 접두사 **없이** 둡니다.

---

## 7. 알려진 빚

고쳐야 하지만 아직 안 한 것들입니다. 숨기지 않고 적어 둡니다.

| | 내용 | 영향 |
|---|---|---|
| **검증 기준값** | `/tmp` 에 의존 | 재부팅하면 `parity` 를 못 돌림 |
| **더미 생성기** | 저장소에 없음 | 데이터를 다시 만들려면 규칙을 보고 새로 짜야 함 |
| **스냅샷 누적** | HTML 7개, 계속 늘어남 | 저장소가 무거워짐 |
| **화면 이식** | 웹앱에 담당자 캘린더만 있음 | 팝업·마케터 화면·캘린더 탭 남음 |
| **웹앱 배포** | 로컬 개발 서버만 | 링크 공유 불가 |

---

## 8. 막혔을 때 보는 곳

| 증상 | 확인 |
|---|---|
| 웹앱이 빈 화면 | `.env.local` 값이 있는지 → `npm run db:check` |
| 숫자가 프로토타입과 다름 | `npm run parity` — 규칙이 갈라진 것 |
| 서버에 반영이 안 됨 | 브라우저 콘솔의 `[db]` 로그. RLS 정책 확인 |
| 배포했는데 그대로 | 해시 대조 (위 **5. 배포**) · 브라우저 강제 새로고침 |
| 캘린더 숫자가 이상 | 점유와 요약은 **다른 숫자**입니다 → [DECISIONS.md](DECISIONS.md) D-03 |
