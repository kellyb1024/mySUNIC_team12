# 추천 알고리즘 기준

2026-08-02 확정 · 적용 파일 `prototype0802_code.html` · `webapp/lib/domain.ts`

마케터가 조회 기간을 넣으면 **그 안에서 시작하기 좋은 날 3개**를 골라 줍니다.

---

## 1. 두 단계로 나눕니다

```
① 거르기   넣을 수 있는 날만 남긴다        ← 하드 룰
② 줄 세우기 남은 날끼리 상대 비교한다        ← 점수
```

**거르는 기준은 캐파 하나뿐입니다.** 시스템이 실제로 막는 것이 그것뿐이기 때문입니다
(2026-07-30 담당자 확인). 시간대는 제약이 아니라 배치의 문제라 여기서 보지 않습니다.

| 거르는 것 | |
|---|---|
| 지난 날짜 | 오늘보다 이전 |
| 캐파 부족 | 그 날(주기성은 기간 내 어느 날이든) 잔여가 신청량보다 적음 |
| 데이터 범위 밖 | 종료일이 데이터 마지막 날을 넘음 |

> **D-1(하루 전) 신청은 거르지 않습니다.**
> 대신 **담당자에게 메일로 알려** 누락을 막습니다. 문제는 "촉박한 건이 들어오는 것"이
> 아니라 "담당자가 못 보고 지나치는 것"이므로, 제약이 아니라 알림으로 풉니다.
> *(메일은 아직 미구현)*

---

## 2. 점수

```
점수 = 0.40 × 혼잡도    + 0.35 × 금·토·일 + 0.25 × 캐파 여유
```

### 혼잡도 (0.40) — 적을수록 좋다

그 날 **같은 채널로 나가는 캠페인 건수**입니다.
캐파가 남아도 같은 날 배너가 다섯 개 나가면 서로 묻힙니다.

주기성이면 기간 중 **가장 몰린 날**을 봅니다.

### 금·토·일 (0.35)

해당하면 1, 아니면 0.

> **주의** — 초기 코드는 반대로 **평일에 가점**을 주고 있었습니다(`주말 ? 0 : 1`).
> 8/2에 뒤집었습니다.

### 캐파 여유 (0.25)

기간 중 **가장 빠듯한 날에 남는 양**입니다. 넉넉할수록 좋습니다.

### 준비 기간은 뺐습니다

원래 `min(1, (오늘~시작일 − 1) / 3)` 으로 0.3을 차지했는데,
**D-2 이상이면 3일 뒤나 30일 뒤나 값이 1**이라 사실상 상수였습니다.
실측에서 후보 전체가 1.0이었습니다. 점수에서 제외했습니다.

---

## 3. 정규화 — 후보군 안에서 상대 비교

혼잡도와 캐파 여유는 **그 조회 기간 안의 최소~최대**로 0~1을 만듭니다.

```
값이 모두 같으면 → 1 (그 항목으로는 순위가 갈리지 않음)
아니면          → (값 − 최소) / (최대 − 최소)
혼잡도는 뒤집습니다 → 1 − 정규화값
```

**왜 이렇게 했나** — 예전에는 한도(배너 400만) 대비로 나눴습니다.
그러면 캠페인이 없는 한산한 구간에서는 어느 날이나 잔여가 400만이라 **값이 같아지고
점수가 동률**이 됐습니다. 결국 날짜순으로만 갈렸습니다.

후보군 안에서 상대 비교하면 **어느 기간을 조회하든 순위가 갈립니다.**

### 동점이면 빠른 날짜가 앞섭니다 — 그리고 3개를 넘으면 잘립니다

```js
.sort((a, b) => b.score - a.score || (a.d < b.d ? -1 : 1)).slice(0, 3)
```

**동점이 3개를 넘어도 화면에는 3개만 나오고, 동점이라는 표시는 없습니다.**
마케터는 이 셋을 「가장 좋은 날 3개」로 읽지만 실제로는 **「동점 중 가장 이른 3개」** 입니다.

드물지 않습니다. seed 204건에서 후보가 4일 이상인 263개 조회를 재 봤습니다 (2026-08-06).

```
3위와 4위가 동점        77건 (29.3%)
동점 무리 크기          3~4일이 가장 흔하고 최대 10일

예) 배너·쿠폰 5일 15만 · 조회 8/16~8/23
    동점 3일(0.600) → 8/16 8/21 8/22 추천, 8/23 은 같은 점수인데 잘림
```

**구조상 그렇습니다.** 항이 셋인데 금·토·일은 0 아니면 1이고, 나머지 둘은 후보군 안
상대 정규화라 한산한 구간에서는 값이 몇 종류밖에 안 나옵니다. 후보 전체의 혼잡도가
같으면 `hi === lo → 1` 이라 `quiet` 이 전원 0인 상수가 되고, 남는 것은 요일뿐이라
그 구간의 금·토·일이 통째로 동점이 됩니다.

**고치지 않았습니다.** 동점끼리는 어느 날을 골라도 점수가 같고 셋 다 신청 가능한
날이므로, 마케터가 손해를 보지 않습니다. 순서가 날짜순으로 정해지는 것이
임의로 정해지는 것보다 낫다고 봤습니다.

> `report/실험_결과.html` R6 은 **개수**만 봅니다 — 「가능한 날이 3개 이상이면 3개를 준다」.
> 동점이 잘리는 것은 그 검사에 걸리지 않습니다. 알고 있는 한계입니다.

---

## 4. 실측

```
배너 10만 · 노출 7일 · 조회 8/10~8/20

바꾸기 전   8/14(금)   8/18(화)   8/20(목)
바꾼 뒤     8/15(토)   8/16(일)   8/14(금)
            1건        1건        3건        ← 같은 채널 건수
            0.968      0.923      0.733      ← 점수
```

```
배너 50만 · 노출 5일 · 조회 9/1~9/30

바꾸기 전   9/2(수)    9/18(금)   9/9(수)
바꾼 뒤     9/5(토)    9/26(토)   9/13(일)
            2건        3건        3건
```

---

## 5. 화면에 무엇이 보이나

```
8/15(토)  추천              잔여 배너 241만 · 쿠폰 440만
8/15(토) ~ 8/21(금)
        [ 이 날짜 선택하기 ]
```

날짜 · 잔여 · 기간만 보여줍니다.
`캐파 빠듯` · `평일 시작` 같은 서술은 뺐습니다 — 금·토·일을 선호하는 지금 기준에서
`평일 시작`은 오해를 부릅니다.

### 근거는 화면에 적지 않습니다 (2026-08-06 결정)

혼잡도는 점수에만 들어가고 카드에는 나오지 않습니다.
카드에 `같은 채널 1건` 한 칸을 더하는 안을 검토했고, **넣지 않기로 했습니다.**

**왜** — 세 후보는 **모두 신청 가능한 날**입니다. 어느 것을 골라도 막히지 않습니다.
순위의 이유를 설명하는 것보다 **화면이 조용한 것**이 낫다고 봤습니다.
사내 실무자가 쓰는 화면이라 서술을 덧붙이지 않는다는 기존 방침과도 같은 방향입니다.

**한계 — 알고 받아들입니다.**

검토 과정에서 *"실무자는 잔여로 유추할 수 있다"* 를 근거로 삼으려 했으나,
측정해 보니 **그 근거는 성립하지 않습니다.**

```
1위가 평일인 조회 23건 (= 혼잡도 때문에 순위가 뒤집혀 설명이 가장 필요한 경우)

  금·토·일 후보가 애초에 없다   3건   ← 설명할 것이 없는 경우

  설명이 필요한 20건 중
    잔여로 유추 가능            6건 (30%)
    잔여가 오히려 반대이거나 같다 1건 ( 5%)
    잔여가 아예 안 뜬다        13건 (65%)
```

| | |
|---|---|
| 잔여가 아예 안 뜨는 조회 | 전체 273건 중 **96건(35%)** · TV팝업·토스트팝업만 쓰고 쿠폰이 없으면 캐파 한도가 없어 표시할 값이 없습니다 |
| 추천 순위와 잔여 순서가 반대인 조회 | 잔여가 뜨는 177건 중 **71건(40%)** · 전체 273건 기준으로는 26% · 혼잡도 0.40 > 캐파 여유 0.25 이므로 당연한 결과입니다 |

**즉 마케터는 대부분의 경우 순위의 이유를 읽어낼 수 없습니다.**
그것을 알고, 그래도 넣지 않기로 했습니다.

> 측정 근거는 `회의/AI멘토링_260806.md` Q5 에 있습니다.

---

## 6. 근거

| 기준 | 출처 |
|---|---|
| 캐파가 유일한 제약 | 2026-07-30 담당자 확인 |
| 마케터는 시간대를 고르지 않음 | 2026-07-31 데모 피드백 |
| 금·토·일 선호 | 2026-08-02 확인 |
| 혼잡도가 낮으면 좋음 | 2026-08-02 확인 |
| 준비 기간 D-2 이상 권고 | 원본 운영 규칙 · 2026-08-02 재확인 |
| D-1 은 메일로 알림 | 2026-08-02 확인 (0728 미팅 피드백 근거) |
| "남은 수 × 위급한 정도로 수식" | 2026-07-28 미팅 |

---

## 7. 고칠 때

### 가중치 — 정본은 한 곳입니다 (2026-08-06)

**`webapp/lib/domain.ts` 의 `REC_W` 하나만 고칩니다.**

HTML 프로토타입은 단일 파일이라 모듈을 못 읽어서 값을 복제해 갖습니다.
그 복제본은 **손으로 고치지 않고 스크립트가 맞춥니다.**

```bash
cd prototype
node verify/rec-weights.mjs          # 두 곳이 같은지 대조 · 다르면 종료 코드 1
node verify/rec-weights.mjs --sync   # 정본 값을 HTML 에 써 넣는다
```

`webapp` 안에서는 `npm run weights` 로도 됩니다.
합이 1이 아니면 같이 걸립니다 — 점수의 최댓값이 1이 아니게 되기 때문입니다.

> 얼린 스냅샷(`prototype0731v2` ~ `0803`)은 대상이 아닙니다.
> 고치지 않는 것이 원칙입니다 (`DECISIONS.md` D-10).

**바꾼 주체는 언제나 개발자입니다.** 담당자에게 조절 화면을 주지 않기로 했습니다
(`회의/기술회의록_260803.md` T-01 · 8/6 확정).

### 계산 자체를 고칠 때

| 파일 | 함수 |
|---|---|
| `prototype0804_code.html` | `REC_W` · `denseOn` · `roomOn` · `recTop` |
| `webapp/lib/domain.ts` | 같은 이름 |

가중치 말고 **계산을 고치면 두 파일을 같이 고쳐야 합니다.**
고친 뒤 `webapp` 에서 `npm run parity` 를 돌리면 둘이 같은 값을 내는지 확인됩니다.
화면까지 안 바뀌었는지는 `verify/README.md` 의 지문 대조로 봅니다.
